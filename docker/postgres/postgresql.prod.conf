# Tuning for the client VPS (4 vCPU / 8 GB). Mounted only by
# docker-compose.prod.yml -- dev runs on PostgreSQL defaults.
shared_buffers = 2GB
effective_cache_size = 4GB
work_mem = 16MB
max_connections = 100
